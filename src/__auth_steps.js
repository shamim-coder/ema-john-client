/**
 * 1. Create a new project at console.google.com
 * 2. Register app
 * 3. npm install firebase
 * 4. create firebase.init.js and import getAuth to export auth.
 * 5. Firebase Setting > Authentication > enable Sign-in providers
 * 6. Create Signup, Login Components, Setup Route
 * 7. set Input Value in State by OnChange, onSubmit> preventDefault()
 * 8. npm install --save react-firebase-hooks
 * 9. useCreateUserWithEmailAndPassword from react-firebase-hooks and import auth
 * 10. Create RequireAuth Component => check user exist also tract user location
 * 11. in route wrap protected component by using RequireAuth component
 */

/**
 * hosting steps
 * 1. npm install -g firebase-tools - (1st time)
 * 2. firebase login - (1st time)
 * 3. firebase init - (one time fore each project)
 * 4. npm run build (build your project) - (every time you want to deploy)
 * 5. firebase deploy - (every time you want to deploy)
 *
 */
